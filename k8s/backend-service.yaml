apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: college-it
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: college-it
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: http
  selector:
    app.kubernetes.io/name: backend
